"use client";

import { Cursor } from "react-creative-cursor";
import "react-creative-cursor/dist/styles.css";

export default function Page() {
  return <Cursor cursorSize={40} animationDuration={0.2} sizeAnimationDuration={0.8} />;
}
